# Environment Variables Template for TalentGuard Buyer Intelligence

# ===================================================
# REQUIRED ENVIRONMENT VARIABLES FOR DEPLOYMENT
# ===================================================

# Airtable Configuration (Legacy - Optional if using Supabase)
AIRTABLE_API_KEY=your_airtable_api_key_here
AIRTABLE_BASE_ID=your_airtable_base_id_here
AIRTABLE_COMPANIES_TABLE=your_companies_table_id
AIRTABLE_CONTACTS_TABLE=your_contacts_table_id
AIRTABLE_SIGNALS_TABLE=your_signals_table_id
AIRTABLE_RESEARCH_TABLE=your_research_table_id
AIRTABLE_ACTIVITIES_TABLE=your_activities_table_id
AIRTABLE_VIEW_ID=your_view_id_here

# Supabase Configuration (REQUIRED for production)
# Get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Database Selection Feature Flag
# Set to 'true' to use Supabase, 'false' to use Airtable
USE_SUPABASE=true
NEXT_PUBLIC_USE_SUPABASE=true

# ===================================================
# REDIS & JOB QUEUE CONFIGURATION
# ===================================================

# Redis Configuration for BullMQ job queues
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
# REDIS_URL=redis://localhost:6379  # Alternative: full Redis connection URL

# ===================================================
# AI & EXTERNAL SERVICE API KEYS
# ===================================================

# OpenAI API Key (for AI research generation)
OPENAI_API_KEY=your_openai_api_key_here

# Perplexity API Key (for news and research)
PERPLEXITY_API_KEY=your_perplexity_api_key_here

# ===================================================
# VERCEL DEPLOYMENT NOTES
# ===================================================

# For Vercel deployment, you MUST add these environment variables 
# to your Vercel project dashboard:
#
# 1. Go to your Vercel project dashboard
# 2. Navigate to Settings > Environment Variables
# 3. Add the following REQUIRED variables:
#    - NEXT_PUBLIC_SUPABASE_URL
#    - NEXT_PUBLIC_SUPABASE_ANON_KEY
#    - USE_SUPABASE
#    - NEXT_PUBLIC_USE_SUPABASE
#
# Optional variables (add if using these features):
#    - OPENAI_API_KEY (for AI research features)
#    - PERPLEXITY_API_KEY (for news analysis)
#    - REDIS_HOST, REDIS_PORT (if using job queues in production)
#
# Note: Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# All others are server-side only

# ===================================================
# LOCAL DEVELOPMENT SETUP
# ===================================================

# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Fill in your actual values in .env.local
#
# 3. Start Redis for job queues (optional):
#    npm run redis:start
#
# 4. Start the development server:
#    npm run dev
#
# 5. Start background workers (optional):
#    npm run workers

# ===================================================
# SECURITY NOTES
# ===================================================

# - Never commit .env.local or actual API keys to version control
# - Use environment-specific values (different keys for dev/staging/prod)
# - Supabase anon keys are safe to expose (they have row-level security)
# - Keep service role keys and database passwords secure
# - Rotate API keys regularly